services:

  postgres:
    image: postgres:latest

    container_name: postgres_smartsalud

    environment:
      POSTGRES_DB: smartsalud
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: contraseña

    ports:
      - "5432:5432"